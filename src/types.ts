 export interface Singer {
  id: number;
  name: string;
  description: string;
  genre: string;
  language: string;
}
 export interface Playlist {
  id: number;
  title: string;
  description: string;
}